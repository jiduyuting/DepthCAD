# Experiment Notebooks

This directory stores exploratory notebooks retained for reference. They are
not part of the main training or evaluation pipeline; reusable code belongs in
`scripts/` and repeatable commands belong in `scripts/runs/`.
