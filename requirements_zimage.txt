# Requirements for Z-Image-Turbo DepthCAD
#
# ⚠️ IMPORTANT: These requirements are DIFFERENT from the original train.py
#
# Key changes:
# - diffusers >= 0.36.0 (for ZImagePipeline and ZImageTransformer2DModel)
# - torch >= 2.1.0 (for compatibility with newer diffusers)
# - transformers >= 4.40.0 (for Qwen3Model support)

# Core dependencies
numpy
pandas
opencv-python
torch>=2.1.0
torchvision
tensorboard
datasets
ftfy

# Training dependencies
accelerate>=0.28.0
transformers>=4.40.0

# ⚠️ CRITICAL: diffusers must be >= 0.36.0 for Z-Image-Turbo
diffusers>=0.36.0

# Optional but recommended
# xformers  # For memory efficient attention (may need specific torch version)
# bitsandbytes  # For 8-bit Adam optimizer

# Note: Installing these requirements may conflict with existing packages.
# It's recommended to use a fresh virtual environment:
#
#   conda create -n depthcad_zimage python=3.10
#   conda activate depthcad_zimage
#   pip install -r requirements_zimage.txt
